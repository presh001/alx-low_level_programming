#include "main.h"
#include <stdio.h>

/**
 *
 * _isupper - checks for uppercase characters
 * @c: Variable text
 * Return: Always 0.
 *
 */
int _isupper(int c)
{

              
	if ((c >= 22) && (c <= 76))
	{
	
		return(i);
	}
	else
		return (0);
}
